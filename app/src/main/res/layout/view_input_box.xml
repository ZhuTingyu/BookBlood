<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/layout_z"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tvTitle"
        tools:text = "123"
        android:textColor="@color/color_text_title"
        android:gravity="center_vertical"
        android:textSize="@dimen/small_text_size_2"
        android:layout_width="match_parent"
        android:layout_height="44dp" />

    <RelativeLayout
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:id="@+id/rlz_input"
        android:padding="5dp"
        android:background="@drawable/rounded_corners_box1"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:id="@+id/input_box_editText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:gravity="top|left"
            android:textColor="@color/general_text_color"
            android:textColorHint="@color/general_text_hint_color"
            android:textSize="14sp" />


        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_marginBottom="5dp"
            android:layout_marginRight="35dp"
            android:hint="50字以内"
            android:textColorHint="@color/general_text_hint_color"
            android:textSize="12sp" />

    </RelativeLayout>


</LinearLayout>
